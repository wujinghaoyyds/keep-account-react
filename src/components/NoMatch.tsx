import React from 'react';
import NavigationBar from './NavigationBar';

function NoMatch() {
    return (
        <div>页面不存在，请输入正确的地址
            <NavigationBar/></div>
    );
}

export default NoMatch;